# Local Development - Connect to Backend
# Leave VITE_API_URL empty to auto-detect based on browser hostname
# When accessing from localhost, it will use localhost:8000
# When accessing from VPS IP (66.42.50.149), it will use 66.42.50.149:8000
# VITE_API_URL=http://localhost:8000/api/v1
VITE_USE_MOCK=false
